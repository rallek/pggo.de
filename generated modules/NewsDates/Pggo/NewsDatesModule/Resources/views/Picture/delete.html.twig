{# purpose of this template: pictures delete confirmation view #}
{% extends routeArea == 'admin' ? 'PggoNewsDatesModule::adminBase.html.twig' : 'PggoNewsDatesModule::base.html.twig' %}
{% block title __('Delete picture') %}
{% block admin_page_icon 'trash-o' %}
{% block content %}
    <div class="pggonewsdatesmodule-picture pggonewsdatesmodule-delete">
        <p class="alert alert-warning">{{ __f('Do you really want to delete this picture: "%name%" ?', {'%name%': picture.getTitleFromDisplayPattern()}) }}</p>

        {% form_theme deleteForm with [
            '@PggoNewsDatesModule/Form/bootstrap_3.html.twig',
            'ZikulaFormExtensionBundle:Form:form_div_layout.html.twig'
        ] %}
        {{ form_start(deleteForm) }}
        {{ form_errors(deleteForm) }}

        <fieldset>
            <legend>{{ __('Confirmation prompt') }}</legend>
            <div class="form-group">
                <div class="col-sm-offset-3 col-sm-9">
                    {{ form_widget(deleteForm.delete) }}
                    {{ form_widget(deleteForm.cancel) }}
                </div>
            </div>
        </fieldset>
        {{ form_end(deleteForm) }}
    </div>
{% endblock %}
