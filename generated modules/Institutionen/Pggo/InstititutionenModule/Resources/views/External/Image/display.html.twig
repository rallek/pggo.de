{# Purpose of this template: Display one certain image within an external context #}
{{ pageAddAsset('javascript', asset('magnific-popup/jquery.magnific-popup.min.js')) }}
{{ pageAddAsset('stylesheet', asset('magnific-popup/magnific-popup.css')) }}
{{ pageAddAsset('javascript', zasset('@PggoInstititutionenModule:js/PggoInstititutionenModule.js')) }}
<div id="image{$image.id}" class="pggoinstititutionenmodule-external-image">
{% if displayMode == 'link' %}
    <p class="pggoinstititutionenmodule-external-link">
    <a href="{{ path('pggoinstititutionenmodule_image_display', { 'id': image.id }) }}" title="{{ image.getTitleFromDisplayPattern()|e('html_attr') }}">
    {{ image.getTitleFromDisplayPattern()|notifyFilters('instititutionen.filter_hooks.images.filter') }}
    </a>
    </p>
{% endif %}
{% if hasPermission('PggoInstititutionenModule::', '::', 'ACCESS_EDIT') %}
    {# for normal users without edit permission show only the actual file per default #}
    {% if displayMode == 'embed' %}
        <p class="pggoinstititutionenmodule-external-title">
            <strong>{{ image.getTitleFromDisplayPattern()|notifyFilters('instititutionen.filter_hooks.images.filter') }}</strong>
        </p>
    {% endif %}
{% endif %}

{% if displayMode == 'link' %}
{% elseif displayMode == 'embed' %}
    <div class="pggoinstititutionenmodule-external-snippet">
        {% if image.imageMeta|default %}
        <a href="{{ image.imageUrl }}" title="{{ image.getTitleFromDisplayPattern()|e('html_attr') }}"{% if image.imageMeta.isImage %} class="image-link"{% endif %}>
        {% if image.imageMeta.isImage %}
            {% set thumbOptions = attribute(thumbRuntimeOptions, 'imageImage') %}
            <img src="{{ image.image.getPathname()|imagine_filter('zkroot', thumbOptions) }}" alt="{{ image.getTitleFromDisplayPattern()|e('html_attr') }}" width="{{ thumbOptions.thumbnail.size[0] }}" height="{{ thumbOptions.thumbnail.size[1] }}" class="img-thumbnail" />
        {% else %}
            {{ __('Download') }} ({{ image.imageMeta.size|pggoinstititutionenmodule_fileSize(image.image.getPathname(), false, false) }})
        {% endif %}
        </a>
        {% endif %}
    </div>

    {# you can distinguish the context like this: #}
    {# % if source == 'contentType' %}
        ...
    {% elseif source == 'scribite' %}
        ...
    {% endif % #}

    {# you can enable more details about the item: #}
    {#
        <p class="pggoinstititutionenmodule-external-description">
            {% if image.description is not empty %}{{ image.description }}<br />{% endif %}
        </p>
    #}
{% endif %}
</div>
