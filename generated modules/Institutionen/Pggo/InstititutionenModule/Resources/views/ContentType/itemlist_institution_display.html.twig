{# Purpose of this template: Display institutions within an external context #}
{% for institution in items %}
    <h3>{{ institution.getTitleFromDisplayPattern() }}</h3>
    <p><a href="{{ path('pggoinstititutionenmodule_institution_display', { 'id': institution.id }) }}">{{ __('Read more') }}</a>
    </p>
{% endfor %}
